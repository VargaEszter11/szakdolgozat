# Python cache files
__pycache__/
*.py[cod]
*$py.class
*.pyc

# Virtual environment
venv/
env/
ENV/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

#Environment variables
.env
*.env
backend/.env

# Uploaded visit photos (created at runtime)
uploads/

# Local-only Docker Compose override (e.g. exposing db port for pgAdmin).
# docker compose auto-merges this file, so committing it would also affect
# the Coolify deployment (which would expose the db port publicly).
docker-compose.override.yaml

# Tests 
tests/__pycache__/
tests/backend/__pycache__/
.coverage